#!/usr/bin/env sh

# Download a minecraft forge mdk project in the current directory.

set -e

version=${1:-`minecraft-version-latest`}

zip=`package=mdk get-minecraft-forge "$version"`

unzip "$zip"
gradle setupDecompWorkspace
