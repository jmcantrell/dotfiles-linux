#!/usr/bin/env bash

# Remove all browser cache files for vimb.

set -eu

cache_dir=${XDG_CACHE_HOME:-$HOME/.cache}
config_dir=${XDG_CONFIG_HOME:-$HOME/.config}

killall vimb

rm -rfv "$cache_dir"/vimb
rm -fv "$config_dir"/vimb/{closed,command,cookies,history,hsts,search}
