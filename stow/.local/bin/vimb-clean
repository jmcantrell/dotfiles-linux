#!/usr/bin/env bash

# Remove all browser cache files for vimb.

set -eu

killall vimb

rm -rfv "${XDG_CACHE_HOME:-$HOME/.cache}"/vimb
rm -fv "${XDG_CONFIG_HOME:-$HOME/.config}"/vimb/{closed,command,cookies,history,hsts,search}
