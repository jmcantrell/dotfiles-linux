#!/usr/bin/env bash

# Add an url to the vimb queue.

set -eu

url=${1:?Missing URL}

queue=${XDG_CONFIG_HOME:-$HOME/.config}/vimb/queue

mkdir -p "$(dirname "$queue")"

if ! grep -wq "$url" "$queue"; then
    printf "%s\n" "$url" >>"$queue"
fi
