#!/usr/bin/env bash

# Set desktop background to a random image from a directory.
# Usage: randbg [DIRECTORY]

set -eu

setbg "$(find "${1:-${XDG_DATA_HOME:-$HOME/.local/share}/backgrounds}" -type f | shuf -n1)"
