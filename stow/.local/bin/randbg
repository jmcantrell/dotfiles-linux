#!/usr/bin/env bash

# Set desktop background to random image from a directory.
# Usage: randbg [DIRECTORY]

set -euo pipefail

setbg "$(find "${1:-$HOME/.local/share/backgrounds}" -type f | shuf -n1)"
