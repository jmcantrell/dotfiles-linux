#!/usr/bin/env bash

# Enable and set an output to be oriented relative to primary.
# Usage: xrandr-orient <edge>

set -euo pipefail

primary=$(xrandr-primary)

edge=$1 # left-of, right-of, above, below
shift

xrandr-detect --"$edge" "$primary" "$@"
