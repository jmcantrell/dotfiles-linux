#!/usr/bin/env bash

set -eu

prompt=input
swaymsg=(swaymsg)

while getopts "p:s:" option; do
    case $option in
    p) prompt=$OPTARG ;;
    s) swaymsg+=(-s "$OPTARG") ;;
    esac
done && shift $((OPTIND - 1))

format=$1
shift

reply=$(bemenu -p "$prompt" </dev/null)
printf -v message "$format" "$reply"
exec "${swaymsg[@]}" "$message"
