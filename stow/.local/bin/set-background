#!/usr/bin/env bash

# Set a desktop background.

set -eu

image=${1:-}
background=$HOME/.background

if [[ -n $image ]]; then
    cp -vL "$image" "$background"
fi

if [[ -v SWAYSOCK && -r $background ]]; then
    swaymsg output "*" background "$background" fill "#000000"
fi
