#!/usr/bin/env bash

# Enable any connected outputs.

set -eu

xrandr | grep '\bconnected\b' | cut -d' ' -f1 |
    xargs -r -I{} xrandr --output "{}" --auto "$@"

# refresh background image in case of resize
setbg
