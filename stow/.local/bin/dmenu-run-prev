#!/usr/bin/env bash

# Execute the last command run from dmenu-run-mru.

set -e

mru_file=${XDG_CACHE_HOME:-$HOME/.cache}/dmenu/run-mru

[[ -f $mru_file ]] || exit 0

mru=$(head -n1 "$mru_file")
echo "$mru" >&2
exec "$mru"
