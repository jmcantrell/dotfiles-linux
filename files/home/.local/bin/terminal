#!/usr/bin/env bash

# Run the preferred terminal emulator.

set -euo pipefail

options=()

if command -v sway-pwd && cwd=$(sway-pwd); then
    options+=(--working-directory="$cwd")
fi

exec foot "${options[@]}" "$@"
