#!/usr/bin/env bash

# Toggle the Mullvad VPN connection.
# This is primarily used for swaystatus interaction.

set -eu

if mullvad-connected; then
    action=stop
else
    action=start
fi

sudo systemctl "$action" wg-quick@wg-mullvad.service
