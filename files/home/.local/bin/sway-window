#!/usr/bin/env bash

# Gets the properties for a given window ID.
# Usage: sway-window ID

set -euo pipefail

swaymsg -t get_tree |
    jq --argjson id "${1:?missing window id}" '.. | select(.id? == $id)'
