#!/usr/bin/env bash

# Unmount menu-selected udiskie device.

set -eu

target=$(
    udiskie-info -a -o "{mount_path}" -f is_mounted |
        sort | bemenu -l 20 -b -p unmount
)

udiskie-umount "$target"
