bindsym      $super+Delete exec bemenu-secrets -c choose
bindsym ctrl+$super+Delete exec bemenu-secrets -c generate

# Show a QR code instead of copying to clipboard.
bindsym      $alt+$super+Delete exec bemenu-secrets -q choose
bindsym ctrl+$alt+$super+Delete exec bemenu-secrets -q generate

# vi:ft=sway
