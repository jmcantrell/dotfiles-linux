bindsym      $super+d exec terminal-dict "$(wl-paste -np)"
bindsym ctrl+$super+d exec terminal-dict "$(wl-paste -n)"
bindsym $alt+$super+d exec terminal-dict "$(bemenu -p define </dev/null)"

# vi:ft=sway
