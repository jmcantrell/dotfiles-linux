set $environment_vars LANG DISPLAY WAYLAND_DISPLAY SWAYSOCK XDG_CURRENT_DESKTOP

exec_always dbus-update-activation-environment --systemd $environment_vars
exec_always systemctl --user import-environment $environment_vars
exec_always systemctl --user start sway-session.target
exec_always kanshictl reload

# vi:ft=sway
