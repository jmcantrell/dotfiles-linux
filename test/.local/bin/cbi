#!/usr/bin/env bash

# Command abstraction for copying text to clipboard.

copy() {
	# copy to primary selection and clipboard
	xsel -i; xsel -o | xsel -ib
}

if (( $# > 0 )); then
	# copy arguments passed
	echo "$@" | copy
else
	# otherwise copy stdin
	copy
fi
