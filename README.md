## Makefile Tasks

```sh
make init     # alias for `make install config stow`
make install  # install system dependencies
make config   # configure system
make stow     # symlink files to home directory
make unstow   # remove symlinks from home directory
```
